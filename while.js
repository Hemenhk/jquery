/**
 * The 'while' loop in JS is used to repeatedly execute a block of code
 * as long as a certain condition is met. 
 */

let count = 0;

while (count < 10) {
    console.log(count);
    count++;
}

